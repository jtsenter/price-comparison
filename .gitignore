__pycache__/
*.pyc
.env
.DS_Store

# Dated daily price snapshots are a write-only local archive (nothing reads them).
# Keep them on disk for reference but don't commit — they bloat the repo ~350KB/day.
docs/data/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9].json

# Local backups / scratch
*.bak
shopping_list.xlsx.bak
scripts/_diag_prices.py
scraper_output.txt
